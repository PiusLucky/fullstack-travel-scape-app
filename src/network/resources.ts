const apiResources = {
  login: "/login",
  register: "/register",
  orders: "/orders",
  packages: "/packages",
  statistics: "/statistics",
  transactions: "/transactions",
  users: "/users",
};

export default apiResources;
