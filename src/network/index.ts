import ApiClient from './axiosClient';
import CookieStorageManager from './cookieStorageManager';

export const apiClient = new ApiClient();

export const cookieStorageManager = new CookieStorageManager();
